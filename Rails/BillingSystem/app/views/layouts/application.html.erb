<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BillingSystem</title>
    <%= csrf_meta_tags %>

    <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>

  <header class="container">
    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to "Billing System", root_path, class: "navbar-brand" %>

          <button class="navbar-toggle" data-toggle="collapse" data-target="#toggler">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div id="toggler" class="collapse navbar-collapse">
          <ul class="nav navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active"><%= link_to "About", about_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to "Contacts",  contacts_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to "Help", help_path, class: "nav-link" %></li>
          </ul>

          <% if signed_in? %>
          <%#= current_user.email %>
          <ul class="nav navbar-nav navbar-right mr-auto mt-2 mt-lg-0">
            <li class="nav-item active"><%= link_to "Activities", activities_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to "Customers", customers_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to "Bills", bills_path, class: "nav-link" %></li>
            <li class="nav-item active"><%= link_to 'Sign out', sign_out_path, method: :delete, class: "nav-link" %></li>
          </ul>
          <% else %>
          <ul class="nav navbar-nav navbar-right mr-auto mt-2 mt-lg-0">
            <li class="nav-item active"><%= link_to 'Sign in', sign_in_path %></li>
          </ul>
          <% end %>
        </div>
      </div>
    </nav>
  </header>

  <section class="container">
    <div id="flash">
      <% flash.each do |key, value| %>
      <div class="flash alert <%= alert_class(key) %>"><%= value %></div>
      <% end %>
    </div>

    <%= yield %>
  </section>
</html>
